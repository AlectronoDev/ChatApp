/**
 * ChannelList is intentionally minimal — channel navigation is handled
 * directly inside Sidebar's ServerEntry component to keep the tree simple.
 * This file is kept as a re-export hook for any future standalone use.
 */
export { default } from './Sidebar';
